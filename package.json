{
  "name": "object-to-promise",
  "description": "Convert an object to a promise.",
  "repository": "git://github.com/micro-js/object-to-promise.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
