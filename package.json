{
  "name": "@micro-js/object-to-promise",
  "description": "Convert an object to a promise.",
  "repository": "git://github.com/micro-js/object-to-promise.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish"
  },
  "dependencies": {
    "@micro-js/values": "^1.0.0",
    "@micro-js/zip-obj": "^1.0.0"
  },
  "devDependencies": {
    "standard": "^5.4.1",
    "tape": "^4.2.2"
  }
}
